import React from 'react';

// import { Container } from './styles';

export default function Login(props) {
  return (
    <div>
        <h1>Login Page</h1>
        {props.children}
    </div>
  );
}
